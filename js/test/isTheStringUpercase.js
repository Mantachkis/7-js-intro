String.prototype.isUpperCase = function() {
    var string=this;
    let newString=string.slice('');
    for(let i=0;i<this.lenght;i++){
      if(newString[i]==newString[i].isUpperCase)
        {}
      else{}
      
      
    }
    
  }